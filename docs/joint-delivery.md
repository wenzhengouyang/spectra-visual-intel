# 早晨统一发布

自 2026-09-11 起，内容审核与质量校验通过后触发统一发布：推送网站，确认线上页面的期次，再发送钉钉。无需分别审批两个渠道。

GitHub Pages 部署异步，通知会等待网页更新；现有每 30 分钟运行的发送任务负责补偿检查和失败重试。发送记录防止同一期重复发送，进程锁防止完成回调与定时任务并发发送。

审核暂停会阻止统一发布。已成功发布网页但发送失败时只补发消息。今天的历史期次不会因启用规则而补发。极端网络情况下，远端已接收但本地未收到响应可能导致重试重复，这是钉钉接口不提供幂等键的限制。
