# 2026-09-11 上午链路复盘

结论：今天通过人工介入完成有限覆盖发布，不是无人值守全链路验收通过。13:20 钉钉成功；原始来源成功率8/36、评测fail均保留。不得把补采结果回填成早晨已成功。

| 阻塞 | 原因与证据 | 已处理 | 缺少的兜底 |
|---|---|---|---|
| 26个来源DNS失败 | 受限执行环境getaddrinfo报errno8；正常本机权限同域名可解析，补采已有恢复；不能断言全部历史故障同因 | 保留安全URL校验，在可联网环境定向补采；DNS错误增加域名、异常类型和errno | 启动前网络预检、环境级熔断、恢复后有限重试；避免批量失败仍显示采集完成 |
| VentureBeat 429 | 来源站点限流，不是补搜账户余额不足 | 保留限流及失败记录 | 冷却后有限重试，不换IP绕过限制 |
| 微信降级 | DNS网络错误、旧缓存和正文获取失败；泛化提示却称授权过期 | 未把缓存算实时刷新 | 区分网络失败与真实凭证过期，通知准确原因 |
| 中文化人工审核无弹窗 | 后台阶段变化未稳定接入通知；非事实审核仅打开状态命令；通知启动即标shown | 本次人工补弹窗，用户批准2条 | 状态变化触发、对应审核入口、通知成功确认及有上限重试 |
| 配图错过3次窗口 | 10:26仍在中文化；固定唤醒结束后才进入配图 | 用户额外授权，5张原文/官方图已保存和实看审核 | 以任务就绪启动有限重试窗口；必须另获用户同意改变调度规则 |
| 图片已审又待审 | 重建稿件丢失审核状态 | 用具体ID、URL、文件指纹、语义类型恢复审核；内容提示变化不复用 | 回归测试覆盖恢复链路 |
| 原图被拒绝 | 生产端支持source，最终校验枚举未支持 | 加入source，仍要求来源署名和使用依据 | 各阶段共用数据契约 |
| Maven正文太薄 | 1条事实37字却分为行业信号，未达到220字/3事实要求 | 用户同意降为普通短讯，持久化单期决定 | 分类时先校验内容厚度，不等最终发布才拦截 |
| 语义审核耗时 | 6篇顺序本地模型审核，单篇约10–113秒 | 6篇最终通过并复用同版本结果 | 阶段耗时预算、超时通知；不能以估计时间当成功承诺 |
| 11:08最终评测失败 | 来源成功率22.2%，发布前才形成阻塞 | 用户明确授权仅今日有限覆盖发布；公开披露，保留fail | 采集后提前决策全量/有限版；异常通知必须可见 |
| 网页上传后钉钉等待 | Pages异步部署，上传不等于网页已更新 | 验证线上当期后13:20发送成功 | 有限部署轮询及回执通知，不依赖人工再触发 |

## 验收边界

补采使用独立快照，不覆盖当期已发布集合，不自动重发。重评必须区分“来源恢复评测”和“新增内容全链路评测”：补来的文章尚需去重、事实审核、编辑及语义检查，不能沿用旧稿审核批准。

现有兜底主要是“暂停、缓存、断点与防重复”，缺少“异常识别→准确通知→有界恢复→重新验证”的完整闭环。安全和事实门禁应保留；修复方向不是关闭所有门禁。
