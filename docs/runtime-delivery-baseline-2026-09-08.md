# SPECTRA 运行与触达基线｜2026-09-08

## 基线范围

- 钉钉发布后推送：登录后每 30 分钟轻量检查，只有当日任务完成且页面已发布才发送，成功后按日去重。
- 预发保护：本次部署将 `daily-20260908` 标记为 `suppressed`，不补发当天消息；未来正式推送保持开启。
- WeRSS 常驻服务：随 macOS 用户登录启动并持续保活，异常或正常退出后均由 `launchd` 重新拉起。
- 统一安装：`install_local_runtime.py` 同步代码后同时加载日更、钉钉和 WeRSS 三个用户服务。

## 审核中修复的问题

1. 原钉钉任务只在 10:00 检查一次，人工审核晚于 10:00 时会漏发；现改为周期检查并依赖发送标记去重。
2. 原 WeRSS 只在异常退出后重启，正常退出仍可能静默停止；现改为持续保活。
3. 原 WeRSS 需要单独安装；现已并入统一运行环境安装入口。

## 验证结果

- 自动化测试：241 项全部通过。
- 两个 `launchd` 模板语法检查通过。
- WeRSS 健康检查返回 HTTP 200。
- WeRSS 强制退出后自动恢复已验证。
- 钉钉真实当日产物预览生成通过。
- 当日 `suppressed` 标记阻止真实补发的回归测试通过。
- 运行环境探针：`ready`。

## 部署状态

- 日更服务：已加载。
- 钉钉服务：已加载；预发验收不补发 2026-09-08，未来正式推送开启。
- WeRSS 服务：已加载并持续运行。
- 页面与 `daily-20260908` 正式发布产物：未在本次基线中改动。
